def backtracking(params):
    if base_case_condition:
        result.append(copy_of_solution)
        return
    
    for choice in choices:
        if violates_constraint:
            continue
        
        make_choice
        backtrack(updated_params)
        undo_choice